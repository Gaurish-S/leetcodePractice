class Solution:
    def topKFrequent(self, nums: List[int], k: int) -> List[int]:
        k_freq = []
        for i in nums:
            if len(k_freq) < k:
                
            if len(k_freq) < k:
                for i in range(k):
